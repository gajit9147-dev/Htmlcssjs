let t = document.body.firstElementChild.firstElementChild


// JavaScript DOM properties (table object)

// Agar aap JS se table access kar rahe ho:

// table.rows → all rows collection

// table.tBodies → tbody list

// table.tHead → thead

// table.caption → caption element

// row.cells → row ke cells

// table.insertRow(index) / row.insertCell(index)

// table.deleteRow(index) / row.deleteCell(index)

console.log(t)

console.log(t.rows)
console.log(t.tbody)
console.log(t.thead)